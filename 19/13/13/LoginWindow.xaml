<Window x:Class="CarRental.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CarRental"
        Title="Вход / Регистрация" Height="360" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Авторизация" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="Логин" Foreground="#37474F"/>
        <TextBox x:Name="UsernameBox" Grid.Row="2" Margin="0,4,0,10" MinHeight="28"/>

        <TextBlock Grid.Row="3" Text="Пароль" Foreground="#37474F"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="0,4,0,10" MinHeight="28"/>

        <StackPanel Grid.Row="5" Margin="0,4,0,0">
            <TextBlock Text="Роль (для регистрации)" Foreground="#37474F"/>
            <ComboBox x:Name="RoleBox" Margin="0,4,0,0" MinHeight="28" SelectedIndex="0">
                <ComboBoxItem Content="Клиент" Tag="{x:Static local:UserRole.Client}"/>
                <ComboBoxItem Content="Менеджер" Tag="{x:Static local:UserRole.Manager}"/>
            </ComboBox>
            <TextBlock x:Name="HintText" Visibility="Collapsed"/>
        </StackPanel>

        <UniformGrid Grid.Row="6" Columns="3" Margin="0,14,0,0">
            <Button Content="Регистрация" Margin="0,0,6,0" Padding="10,6" Click="Register_Click"/>
            <Button Content="Войти" Margin="3,0,3,0" Padding="10,6" IsDefault="True" Click="Login_Click"/>
            <Button Content="Закрыть" Margin="6,0,0,0" Padding="10,6" IsCancel="True"/>
        </UniformGrid>
    </Grid>
</Window>
